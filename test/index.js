'use strict';

var should = require('should');
var app-config-chain = require('../');

describe('FUNCTIONNAME()', function() {
  it('should TASKNAME', function(done) {
    should.exist(app-config-chain);
    done();
  });
});
